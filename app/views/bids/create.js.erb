console.log("<%= number_to_currency(@bid.item.highest_bid_amount) %>")
$("#high_bid_<%= @bid.item_id %>").text("<%= number_to_currency(@bid.item.highest_bid_amount) %>")
//number_to_currency(item.highest_bid_amount)
